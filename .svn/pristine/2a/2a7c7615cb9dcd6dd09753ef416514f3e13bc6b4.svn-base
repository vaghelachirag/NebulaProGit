package com.nebulacompanies.ibo.ecom.fragment;

import android.app.LauncherActivity;
import android.content.pm.ActivityInfo;
import android.os.Build;
import android.os.Bundle;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import androidx.annotation.Nullable;
import androidx.fragment.app.Fragment;
import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;


import java.util.ArrayList;
import java.util.List;

import static android.provider.MediaStore.Images.ImageColumns.ORIENTATION;


public class MyOrdersFragment extends Fragment {

   /* //a list to store all the products
    List<OrderDetailsModel> orderDetailsModels;

    //the recyclerview
    RecyclerView recyclerView;

    List<LauncherActivity.ListItem> items;
    public static MyOrdersFragment newInstance(String param1, String param2) {
        MyOrdersFragment f = new MyOrdersFragment();
        return f;
    }

    @Nullable
    @Override
    public View onCreateView(LayoutInflater inflater, @Nullable ViewGroup container, @Nullable Bundle savedInstanceState) {
        View view = inflater.inflate(R.layout.content_my_orders, container, false);

        if (android.os.Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            getActivity().setRequestedOrientation(ActivityInfo.SCREEN_ORIENTATION_PORTRAIT);
        }



        return view;
    }

    @Override
    public void onViewCreated(View view, @Nullable Bundle savedInstanceState) {
        int orientation =getActivity().getIntent().getIntExtra(ORIENTATION, LinearLayoutManager.VERTICAL);

       *//* RecyclerView recycler = (RecyclerView) view.findViewById(R.id.recycler_view);
        recycler.setLayoutManager(new LinearLayoutManager(getActivity(), orientation, false));

        //adapter = new TimelineAdapter(orientation, getItems());
        adapter = new TimelineAdapter(orientation, setDataListItems());
        recycler.setAdapter(adapter);*//*
        //setDataListItems();

        recyclerView = (RecyclerView) view.findViewById(R.id.recycler_view);
        recyclerView.setHasFixedSize(true);
        recyclerView.setLayoutManager(new LinearLayoutManager(getActivity()));

        //initializing the productlist
        orderDetailsModels = new ArrayList<>();


        //adding some items to our list
        orderDetailsModels.add(
                new OrderDetailsModel(
                        "Arriving Soon",
                        "Order Dispatched", "https://encrypted-tbn0.gstatic.com/images?q=tbn%3AANd9GcQpPD6FKAHQakqfzczGwMS3rNq80hmEixZezhRvhHOjmYQgpJyF&usqp=CAU"));

        orderDetailsModels.add(
                new OrderDetailsModel("Arriving Soon",
                        "On the way", "https://img.mensxp.com/media/shop/catalog/products/F/708972/f9-pre-workout-in-fruit-punch-flavour-50-servings-400-gm-107553-default.jpg"));

        orderDetailsModels.add(
                new OrderDetailsModel("Order Delivered",
                        "08th Jan,2020", "https://www.enomark.com/admin/images/composition_images/LTSYP-105_0_1545215812.jpg"));

        orderDetailsModels.add(
                new OrderDetailsModel("Order Delivered",
                        "08th Oct,2019", "https://nutrifusion.com/wp-content/uploads/2018/02/NF-216-ProductPhoto-min.png"));


        //creating recyclerview adapter
        OrderListAdapter adapter = new OrderListAdapter(getActivity(), orderDetailsModels);

        //setting adapter to recyclerview
        recyclerView.setAdapter(adapter);
    }*/
}
